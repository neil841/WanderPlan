# Database
# Connection pooling is automatically configured by Prisma
# For production, consider using a connection pooler like PgBouncer
DATABASE_URL="postgresql://username:password@localhost:5432/wanderplan"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="" # Generate with: openssl rand -base64 32

# Email (Resend)
RESEND_API_KEY="" # Get from resend.com - Free tier: 3,000 emails/month
FROM_EMAIL="noreply@wanderplan.com" # Your verified sending domain
FROM_NAME="WanderPlan"

# File Upload (Vercel Blob)
BLOB_READ_WRITE_TOKEN=""

# Stripe (Payment Processing)
STRIPE_SECRET_KEY="" # Get from Stripe Dashboard (use sk_test_... for development)
STRIPE_PUBLISHABLE_KEY="" # Get from Stripe Dashboard (use pk_test_... for development)
STRIPE_WEBHOOK_SECRET="" # Get from Stripe Webhooks (use whsec_... after creating webhook endpoint)
NEXT_PUBLIC_APP_URL="http://localhost:3000" # Your app URL (for Stripe redirects)

# External APIs
OPENWEATHER_API_KEY="" # Optional: for weather forecasts
FOURSQUARE_API_KEY="" # Optional: for POI search

# Google Calendar Integration (Optional)
GOOGLE_CLIENT_ID="" # Get from Google Cloud Console - OAuth 2.0 Client ID
GOOGLE_CLIENT_SECRET="" # Get from Google Cloud Console - OAuth 2.0 Client Secret
GOOGLE_REDIRECT_URI="" # OAuth redirect URI (default: {NEXTAUTH_URL}/api/integrations/google-calendar/callback)

# App Config
NODE_ENV="development"
